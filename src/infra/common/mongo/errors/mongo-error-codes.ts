/**
 * All error codes from MongoDB that we need to handle, you can add new codes as needed.
 *
 * For a full list of error codes you can access this link
 * @link [Mongo Error Codes Docs](https://github.com/mongodb/mongo/blob/master/src/mongo/base/error_codes.yml)
 */
export const enum MONGO_ERRORS {
  DUPLICATED_KEY = 11000,
}
